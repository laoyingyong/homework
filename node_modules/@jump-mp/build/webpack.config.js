var path = require('path');
const copyWebpackPlugin=require('copy-webpack-plugin');

module.exports = {
  entry: path.resolve(__dirname, './src/index.js'),
  output: {
    path: path.resolve(__dirname, './dist'),
    libraryTarget: "umd",
    filename: "index.js",
  },
  target: 'node',
  node:{
      fs:'empty',
      child_process:'empty',
      __dirname: false
  },
  externals: {
    webpack: 'webpack',
  },
  devtool : "source-map",
  plugins:[
    new copyWebpackPlugin(
      {
        patterns: [
          {
            from: path.resolve(__dirname, './src/template'),
            to: path.resolve(__dirname, './dist/public/template')
          },
        ],
      }
    )
  ],
  module: {
    rules: [
      // {
      //   test: /\.node$/,
      //   loader: 'node-loader',
      // },
    ]
  }
}
